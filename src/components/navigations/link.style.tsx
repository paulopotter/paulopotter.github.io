import { createUseStyles } from "react-jss";

export const LinkStyle = createUseStyles({
  icon: {
      display: 'inline-block',
      height: '.8em',
      verticalAlign: 'top',
      width: '.9em',
  }
});
