import { createUseStyles } from 'react-jss';

export const HomeStyle = createUseStyles({
  content: {
    display: 'flex',
    'flex-direction': 'row',
    'flex-wrap': 'wrap',
    'justify-content': 'center',
  },
});
